{
  "name": "@mauntic/revops-queue",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "build": "wrangler deploy --dry-run --config wrangler.toml",
    "typecheck": "tsc --noEmit -p ../revops/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^4.67.0"
  }
}
