{
  "name": "@mauntic/delivery-queue",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "build": "wrangler deploy --dry-run --config wrangler.toml",
    "typecheck": "tsc --noEmit -p ../delivery/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^4.65.0"
  }
}
