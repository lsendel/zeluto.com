/**
 * Mauntic Identity Worker
 *
 * Handles authentication and authorization using Better Auth.
 * Provides session management, OAuth providers, and organization support.
 */
import app from './app.js';

export default app;
