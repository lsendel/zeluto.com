# Rspamd Milter Headers Configuration
# Controls which headers Rspamd adds to scanned messages

use = ["x-spamd-bar", "x-spam-level", "x-spam-status", "authentication-results"];

# Add extended spam headers for processing by the mail-api
extended_spam_headers = true;

# Authentication-Results header
authenticated_headers = ["authentication-results"];
